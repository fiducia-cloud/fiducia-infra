# Copy to .env (gitignored; loaded by .envrc via direnv, or `source .env` inside
# `./shell`). These are the standard env-auth names read by BOTH the provider
# CLIs (hcloud/civo/vultr-cli) and terraform/envs/prod's providers.
HCLOUD_TOKEN=
VULTR_API_KEY=
CIVO_TOKEN=
