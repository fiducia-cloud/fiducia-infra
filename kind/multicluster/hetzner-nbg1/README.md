# Hetzner NBG1 emulator member

This is a disposable Kind cluster labelled as Hetzner NBG1. It is one member of
the default `hetzner-regions` test profile; it does not call Hetzner APIs or
claim to reproduce a physical provider failure domain. Run the complete profile
from its parent directory with `./up.sh`.
