apiVersion: v1
kind: Service
metadata:
  name: fiducia-operator
  namespace: fiducia
  labels:
    app: fiducia-operator
spec:
  selector:
    app: fiducia-operator
  ports:
    - name: http
      port: 8080
      targetPort: http
