apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: fiduciaclusters.operator.fiducia.cloud
spec:
  group: operator.fiducia.cloud
  names:
    kind: FiduciaCluster
    plural: fiduciaclusters
    shortNames:
    - fcluster
    singular: fiduciacluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for FiduciaClusterSpec via `CustomResource`
        properties:
          spec:
            properties:
              brainStatefulSet:
                description: Optional local brain member. Node-only clusters leave this unset.
                nullable: true
                type: string
              nodeStatefulSet:
                default: fiducia-node
                description: Local data-plane `StatefulSet`. The observer never changes it.
                type: string
              requeueSeconds:
                default: 30
                description: Periodic resync protects against a missed watch event.
                format: uint64
                maximum: 3600.0
                minimum: 10.0
                type: integer
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  properties:
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              mutationEnabled:
                type: boolean
              observedGeneration:
                format: int64
                nullable: true
                type: integer
              phase:
                type: string
              workloads:
                items:
                  properties:
                    boundPvcs:
                      format: int32
                      type: integer
                    exists:
                      type: boolean
                    expectedPvcs:
                      format: int32
                      type: integer
                    expectedReplicas:
                      format: int32
                      type: integer
                    issues:
                      items:
                        type: string
                      type: array
                    name:
                      type: string
                    readyReplicas:
                      format: int32
                      type: integer
                    rolloutGuarded:
                      type: boolean
                    storageContract:
                      type: boolean
                  required:
                  - boundPvcs
                  - exists
                  - expectedPvcs
                  - expectedReplicas
                  - issues
                  - name
                  - readyReplicas
                  - rolloutGuarded
                  - storageContract
                  type: object
                type: array
            required:
            - conditions
            - mutationEnabled
            - phase
            - workloads
            type: object
        required:
        - spec
        title: FiduciaCluster
        type: object
    served: true
    storage: true
    subresources:
      status: {}
