{
  "name": "fiducia-edge",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "devDependencies": {
    "wrangler": "^4.105.0"
  },
  "scripts": {
    "check": "node --check src/index.mjs && node --check src/jwt-revocation-boundary.mjs && node --check src/security-boundary.mjs && node --check src/worker.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test src/*.test.mjs"
  }
}
