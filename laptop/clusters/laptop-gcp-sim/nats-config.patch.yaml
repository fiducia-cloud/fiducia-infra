# GENERATED by tools/render-laptop-messaging.mjs from laptop/topology.toml — do not edit.
# Strategic-merge patch for the hand-authored base ConfigMap. A Kustomize
# generator cannot use behavior=replace against a resource loaded from YAML.
apiVersion: v1
kind: ConfigMap
metadata:
  name: fiducia-nats-config
  namespace: fiducia
data:
  nats.conf: |
    # GENERATED by tools/render-laptop-messaging.mjs from laptop/topology.toml — do not edit.
    # Three independent single-node K3s clusters form one three-server JetStream
    # cluster over the private mesh. Client and monitoring planes remain local.
    server_name: fiducia-nats-laptop-gcp-sim
    server_tags: ["site:site-b", "cluster:laptop-gcp-sim", "substrate:laptop-k3s"]
    port: 4222
    http: 8222
    
    max_payload: 8MB
    ping_interval: "20s"
    write_deadline: "10s"
    
    jetstream {
      store_dir: "/data/jetstream"
      max_memory_store: 512MB
      max_file_store: 8GB
      max_outstanding_catchup: 64MB
      max_buffered_msgs: 10000
      max_buffered_size: 64MB
      request_queue_limit: 5000
      strict: true
      unique_tag: "site"
    
      limits {
        max_ack_pending: 10000
        duplicate_window: 600s
      }
    }
    
    cluster {
      name: fiducia-laptop-production
      listen: "0.0.0.0:6222"
    
      # Other servers must contact this member through the tailnet mirror rather
      # than a pod IP, host IP, or residential address. no_advertise below applies
      # to client URL gossip; it does not remove this route contact address.
      advertise: "fiducia-nats-route-laptop-gcp-sim-tailnet.fiducia.svc.cluster.local:6222"
      no_advertise: true
      pool_size: 1
    
      tls {
        cert_file: "/etc/nats/route-tls/tls.crt"
        key_file: "/etc/nats/route-tls/tls.key"
        ca_file: "/etc/nats/route-tls/ca.crt"
        min_version: "1.3"
        verify: true
        verify_cert_and_check_known_urls: true
        timeout: 5
      }
    
      routes = [
          nats://fiducia-nats-route-laptop-aws-sim-tailnet.fiducia.svc.cluster.local:6222
          nats://fiducia-nats-route-laptop-azure-sim-tailnet.fiducia.svc.cluster.local:6222
      ]
    }
    
    # Client accounts and subject permissions remain fail-closed and are delivered
    # independently from the route CA/certificate Secret. The include must define a
    # SYS system account and a separate JetStream-enabled application account.
    include ./auth/auth.conf
