# GENERATED by tools/render-laptop-fleet.mjs from laptop/topology.toml — do not edit.
# These are private Tailscale L3 ingress Services, not public cloud load balancers.
apiVersion: v1
kind: Service
metadata:
  name: fiducia-node-peer-tailnet
  namespace: fiducia
  annotations:
    tailscale.com/hostname: node-laptop-azure-sim
    tailscale.com/tags: "tag:fiducia-peer-azure-sim"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: fiducia-node
  ports:
    - { name: peer, port: 9090, targetPort: 9090, protocol: TCP }
---
apiVersion: v1
kind: Service
metadata:
  name: fiducia-brain-peer-tailnet
  namespace: fiducia
  annotations:
    tailscale.com/hostname: brain-laptop-azure-sim
    tailscale.com/tags: "tag:fiducia-peer-azure-sim"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: fiducia-brain
  ports:
    - { name: peer, port: 9095, targetPort: 9095, protocol: TCP }
