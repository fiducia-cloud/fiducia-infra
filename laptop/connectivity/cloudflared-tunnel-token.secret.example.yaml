# STRUCTURAL EXAMPLE ONLY. Keep the real file outside Git and mode 0600.
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-tunnel-token
  namespace: fiducia
type: Opaque
stringData:
  token: "REPLACE_ME_WITH_THE_REMOTELY_MANAGED_TUNNEL_TOKEN"
