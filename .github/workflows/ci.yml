name: ci

on:
  push:
    branches:
      - proof/ores-proc-isolation-2-linux-20260924
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runner-smoke:
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm funded runner
        shell: bash
        run: |
          set -euo pipefail
          echo funded-runner-ok
