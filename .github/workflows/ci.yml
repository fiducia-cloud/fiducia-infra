name: ci

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  proc-isolation-proof:
    uses: ./.github/workflows/certify-ores-proc-isolation-2-linux.yml
    secrets: inherit
