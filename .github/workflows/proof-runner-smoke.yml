name: proof runner smoke

on:
  push:
    branches:
      - proof/ores-proc-isolation-2-linux-20260924
    paths:
      - .github/workflows/proof-runner-smoke.yml

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm runner
        run: echo runner-ok
